name: nixos-update
version: "1.3.0"
description: "Approved tools for queueing NixOS updates and reading bounded service logs."
kind: standalone
provides_tools:
  - nixos_update
  - systemd_service_logs
